// Stub — regenerar com `flutter create .` quando o SDK estiver disponível.
android {
    namespace "app.nadaaqui.mobile"
    compileSdk 34
    defaultConfig {
        applicationId "app.nadaaqui.mobile"
        minSdk 21
        targetSdk 34
    }
}
